# Same-provider continuity preparation

Issue #8 now has a repeatable offline fixture using the actual pinned OpenCode
binary, shared native boundary, 9Router's account-selection code and durable
original receipts. Two same-profile text requests completed on synthetic A then B
following one recorded stock-router model-lock transition. This is preparatory
work; the issue's two-distinct-subscription live criterion remains pending.

The [compact run record](router-continuity-run.json) separates 24 constructed
control/client tests from actual harness/router execution. TypeScript checking
passed. The integrated pair used two physical synthetic sends. Partial output,
router outage, forbidden tool output and intent/result persistence faults used
1, 0, 1, 1 and 1 sends respectively. Partial output remained unknown and its
replacement produced no second send. Every case retained evidence before removing
its disposable containers/volumes. The first attempted pair was correctly refused
because the pin quotes its prompt; that failure was retained and the fixture now
checks the exact captured serialization.

A fresh review found that the first worker entry emitted its observation before
closing the relay. The corrected entry emits only when parked for inspection and
skips the hold after active cancellation. Actual container SIGTERM retained one
unknown original send and exited in 288 ms; delayed Docker start completed the
two-send pair. Neither result treats local exit as an upstream acknowledgement.

The native edit classifier is unchanged. This fixed text result is
`continuity_transport_completed`; it also requires an independently read unchanged
artifact, original EOF, validated durable decision, exact profile/settings and
request/output identities. The optional private transition derives A from the
first durable receipt and accepts no caller-selected account or token. It records
intent before the actual router helper, verifies graph/credentials/scope/debits,
and persists a one-use result before allowing the next request. It refuses
nonquiescence, stale/expired authority and admission races. Persistence uncertainty
fences the gateway. Injected health state is never natural quota evidence.

The shared deployment CLI now packages the opt-in fixture and source manifest and
exposes its private evidence/transition commands. An attach-only consumer verifies
the prepared source and uses the existing initial scope; it cannot create, reset
or restart that allowance. A separate production-source synthetic check used the
actual prepare/start commands, default worker staging and normal gateway loader
without fault injection. Both requests completed on A then B, the same scope's
spent count advanced from zero to two, its start/deadline were unchanged, and stop,
retention and cleanup passed. This demonstrates packaging, not live subscription
continuity.

See the [fixture README](../../experiments/router-continuity/README.md) for commands,
source pins, exact declaration, failure matrix and limitations. Final parent-stack
integration, shared suite sequencing and fresh controls review remain; none of
these offline results activates a live deployment. The
full reviewed initial suite must share one aggregate 10-physical-attempt,
600,000-ms scope with retries charged, no refresh and no paid fallback. Private
proof of two distinct existing Codex subscriptions, credential expiry, concrete
isolation/egress review, #6/#7 live results and actual bounded live continuity are
still required. Usage/headroom and live passive readiness remain unknown. #9's
foundation decision remains a gate to the durable application core.
